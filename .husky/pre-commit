#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Ejecutar lint-staged para formatear y lintear archivos modificados
npx lint-staged

# Verificar tipos de TypeScript
npm run type-check

# Ejecutar tests si existen
if [ -f "jest.config.js" ] || [ -f "jest.config.ts" ]; then
  npm test
fi
